<div class="vertical-center">
    <div class="container">
            <div class="row d-flex justify-content-center" id="showingArtistAlbums">
             <form class="form-inline">
                 <div class="form-group mb-1">
                    <h2 class="mr-5"> Albums List</h2>
                  </div>
              </form>
            </div>
      </div>
  </div>

<%= form_tag("/albums", method: "get") do %>
    <form>
    <div class="col-7">
      <div class="form-row align-items-center">
          <div class="input-group mb-2">
            <div class="input-group-prepend">
              <%= label_tag(:title, "Filter:", class: "input-group-text") %>
            </div>
            <%= text_field_tag(:title, params['title'], placeholder:"Album Title", class: "mr-3") %>
            <%= submit_tag("Submit", class: "btn btn-outline-success mr-2") %>
            <%=link_to "Create Album", new_album_path, class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </form>

<% end %>

  <table class="table table-bordered table-hover" >
  <thead>
    <tr>
      <th scope="col">Album</th>
      <th scope="col">Artist</th>
    </tr>
  </thead>
  <tbody>
    <% @albums.each do |album| %>
    <tr>
      <td><%= link_to album.title, album %></td>
      <td><%= album.artist.name %></td>
    </tr>
  </tbody>
    <% end %>
</table>
